<!DOCTYPE html>
<html>
  <head>
    <title>RailsWatchList</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url('<%=asset_url("fond.jpg") %>');">
  <div class="container">
    <h1>List your favorites movies!</h1>

    <div class="buttons">
      <%= link_to "Add a list", new_list_path, class: "btn btn-primary mt-5 all"  %>
      <br>
      <%= link_to "All lists", lists_path, class: "btn mt-5 all"  %>
    </div>
    
  </div>
</div>
    <%= yield %>
  </body>
</html>
